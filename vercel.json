{
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "install": "curl -fsSL https://bun.sh/install | bash && source ~/.bashrc && export PATH=$HOME/.bun/bin:$PATH && bun install",
    "buildCommand": "bun run build"
  }
}
